<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form</title>
    <script src="http://webdev.it.kmitl.ac.th/lab/bootstrap/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="http://webdev.it.kmitl.ac.th/lab/bootstrap/css/bootstrap.min.css">

    <style>
        body {
            height: 100vh;
            display: flex;
            align-items: center;
        }
        .container {
            display: block;
            justify-content: center;
            box-shadow: 0 0 30px 5px lightgray;
            width: 30%;

        }

        form {
            width: 100%;
            padding: 20px;
        }

        .btn {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 10px;
        }

        a, button {
            border: none;
            background-color: lightcoral;
            padding: 7px;
            border-radius: 5px;
            text-decoration: none;
            color: white;
            font-weight: bold;
        }

        a:hover, button:hover {
            background-color: rgb(130, 205, 231);
            color: rgb(0, 149, 199);
        }

        textarea {
            resize: none;
        }

        label {
            font-weight: bold;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <form action="/save" method="POST">
            <div class="form-group">
                <label>Employee ID:</label><br>
                <input type="text" class="form-control" name="CustomerId" value="<%= data.CustomerId%>">
            </div>

            <div class="form-group">
                <label>Firstname:</label><br>
                <input type="text" class="form-control" name="FirstName" value="<%= data.FirstName%>">
            </div>

            <div class="form-group">
                <label>Lastname:</label><br>
                <input type="text" class="form-control" name="LastName" value="<%= data.LastName%>">
            </div>

            <div class="form-group">
                <label>Address:</label><br>
                <textarea name="Address" class="form-control" id="Address"><%= data.Address%></textarea>
            </div>

            <div class="form-group">
                <label>Email:</label><br>
                <input type="email" name="Email" class="form-control" value="<%= data.Email%>">
            </div>

            <div class="form-group">
                <label>Phone:</label><br>
                <input type="tel" name="Phone" class="form-control" value="<%= data.Phone%>">
            </div>

            <div class="btn">
                <button type="submit" class="btn-group">Save Data</button>
                <a class="btn-group" href="/show">Show Data</a>
                <a class="btn-group" href="/clear">Clear Data</a>
            </div>
        </form>
    </div>
</body>
</html>