<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>show</title>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap');
        body {
            font-family: "Nunito", serif;
            justify-items: center;
        }

        h1 {
            margin-top: 50px;
        }
        .container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            width: 80%;
            gap: 30px;
        }

        .card {
            width: 70%;
            overflow:auto ;
            box-shadow: 0 0 20px 5px lightgray;
            display: flex;
            flex-direction: column;
            padding: 30px;
            border-radius: 10px;
        }

        img {
            width: 100%;
            height: auto;
            margin-bottom: 10px;
        }

        strong {
            font-size: 20px;
        }
    </style>
</head>
<body>
    <h1>List of Countries</h1>
    <div class="container">
        <% data.forEach((item)=>{ %>
            <div class="card">
                <img src="http://10.0.15.21:8000<%= item.flag %>" alt="Flag of China">
                <strong><%= item.name %></strong> <br>
                Capital: <%= item.capital %> <br>
                Region: <%= item.region %> <br>
                Population: <%= item.population %> <br>
                Latlng: <%= item.latlng %> <br>
                borders: <%= item.borders %>
                
            </div>


        <%});%>
    </div>
</body>
</html>